"""Incident Management Process Module

This module contains the Incident class which is used to manage the Incident Management Process.
"""

def create():
    # creating an incident means that based on a series of inputs, either from a slack command that triggers a modal or from a web form, the incident is created and the incident is then managed through a series of steps
    # the flow then goes on to manage the incident through a series of steps


def update_status():
    # status can be open, resolved, closed


def close():
    # closing an incident means that the incident is no longer active and is closed. It will trigger a series of steps to close the incident and notify the relevant stakeholders