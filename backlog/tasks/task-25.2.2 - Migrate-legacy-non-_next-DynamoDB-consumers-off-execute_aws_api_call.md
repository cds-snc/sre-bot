---
id: TASK-25.2.2
title: >-
  Rationalize integrations/aws/client.py: typed Literal-overloaded factory,
  eager AssumeRole, SDK-native retry and timeout policy, settings consolidated
  in integrations/aws/settings.py
status: To Do
assignee: []
created_date: '2026-07-31 18:48'
updated_date: '2026-09-11 17:31'
labels:
  - clients
  - phase-3
milestone: m-3
dependencies:
  - TASK-25.2.1
references:
  - app/integrations/aws/client.py
  - app/integrations/aws/settings.py
  - app/infrastructure/configuration/integrations/aws.py
  - decisions/outbound-clients.md
  - decisions/sdk-typing.md
  - app/infrastructure/storage/service.py
  - app/infrastructure/idempotency/factory.py
  - app/infrastructure/resilience/retry/factory.py
  - app/integrations/google_workspace/client.py
parent_task_id: TASK-25.2
priority: high
ordinal: 119000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Slice 1 of TASK-25.2. Rework get_aws_client into the single AWS client factory the rest of the series builds on, without yet touching the legacy helpers: handle_aws_api_errors, execute_aws_api_call, paginator, assume_role_session and get_aws_service_client stay as they are until TASK-25.2.6 so the mirror modules keep working through the intermediate slices.

Target shape: get_aws_client(service, *, role_arn=None, session_name=..., retries=True) overloaded on Literal service names (dynamodb, identitystore, organizations, sso-admin, ce, config, guardduty, securityhub, lambda, sts) so each call returns the matching types-boto3 client type and call sites need no cast; the session_config/client_config dict kwargs are removed. AssumeRole is done eagerly through the public API (sts.assume_role, then boto3.Session from the returned credentials) instead of writing DeferredRefreshableCredentials into botocore's private _session._credentials; clients are built per operation and never cached, so no credential refresh is needed. Retries use Config(retries={"mode": "standard", "max_attempts": N}) with explicit connect/read timeouts (decisions/outbound-clients.md items 20-23), plus a retries-disabled variant for non-idempotent writes (item 25) that later slices use wherever a replay is unsafe. The dynamodb-local endpoint gate stays, once (botocore's native AWS_ENDPOINT_URL_DYNAMODB variable is the SDK-native replacement if the local/ci environment files can set it; evaluate at planning, do not assume).

Settings (human decision 2026-09-11): integrations/aws/settings.py, colocated with client.py, becomes the single source of every AWS setting for this series. It already defines the transport fields (AWS_REGION, AWS_ENDPOINT_URL, RETRY_MODE, RETRY_MAX_ATTEMPTS, CONNECT/READ timeouts, NOT_FOUND/UNAUTHORIZED/TRANSIENT code catalogues) that client.py today tries to read through getattr defaults from infrastructure/configuration/integrations/aws.py, where they do not exist; wire get_aws_client and classify_aws_error to it. Add to it, with identical field names and env aliases, the feature-level fields the mirrors and the later adapters need (SYSTEM_ADMIN_PERMISSIONS, VIEW_ONLY_PERMISSIONS, AUDIT/ORG/LOGGING_ROLE_ARN, INSTANCE_ID, INSTANCE_ARN, SERVICE_ROLE_MAP) so TASK-25.2.3 onwards read only this module. Mirror modules keep importing the infrastructure module until they are deleted; the infrastructure module and its barrel export are retired in TASK-25.2.6. Do not add AWS fields to infrastructure/configuration (the export barrel there is why they are being moved out).

Callers updated: infrastructure/storage/service.py, infrastructure/idempotency/factory.py and infrastructure/resilience/retry/factory.py drop their casts. packages/access/sync/adapters/aws_identity_center.py keeps working as is (its get_aws_client("identitystore", role_arn=...) call stays valid); dropping its now-redundant cast is a fine incidental edit, but nothing beyond that: the access feature is not enabled yet and its own migration is out of this series' scope.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 get_aws_client is overloaded on Literal service names and returns the types-boto3 client type for each; mypy passes with the casts removed from the storage, idempotency and retry-store callers; packages/access needs at most its redundant cast removed
- [ ] #2 AssumeRole uses sts.assume_role and a public boto3.Session constructor; no private botocore attribute is accessed anywhere under app/integrations
- [ ] #3 Retry mode, max attempts, connect and read timeouts, region and endpoint are read from integrations/aws/settings.py and set once at construction, a retries-disabled option exists, and unit tests assert the botocore Config the factory produces for both variants and that environment overrides take effect
- [ ] #4 classify_aws_error reads its code catalogues from integrations/aws/settings.py and its tests cover each mapped family (not-found, unauthorized, transient with retry_after, ConditionalCheckFailedException permanent, BotoCoreError transient) plus one unmapped ClientError propagating
- [ ] #5 integrations/aws/settings.py carries the feature-level fields (permission sets, role ARNs, SSO instance id/ARN, SERVICE_ROLE_MAP) with the same names and env aliases as the infrastructure module, covered by tests; infrastructure/configuration gains no AWS field
- [ ] #6 The legacy helpers remain callable and every existing mirror-module test still passes
- [ ] #7 The dynamodb-local endpoint is driven by AWSSettings.DYNAMODB_ENDPOINT_URL (env AWS_ENDPOINT_URL_DYNAMODB, botocore's native name) applied only to dynamodb clients; client.py no longer imports infrastructure.configuration.app; .devcontainer/docker-compose.yml sets the variable; unit tests cover set, unset and a non-dynamodb service
- [ ] #8 AWS_ENDPOINT_URL is removed from AWSSettings (shield.py passes endpoint_url=None until its deletion) and classify_aws_error's transient retry hint is AWSSettings.TRANSIENT_RETRY_AFTER_SECONDS (env AWS_TRANSIENT_RETRY_AFTER_SECONDS, default 60), both covered by tests
- [ ] #9 types-boto3 gains the sts extra in pyproject.toml with uv.lock regenerated so the sts overload is typed; the default RoleSessionName is sre-bot and AssumeRole failures propagate as SDK exceptions
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
GROUNDING (2026-09-11, re-grepped; human decisions from planning Q&A recorded inline)
- get_aws_client callers (production): infrastructure/storage/service.py:302 (cast to a LOCAL Protocol DynamoDBClient, service.py:30-37), infrastructure/idempotency/factory.py:29 and :41 (cast to types_boto3_dynamodb DynamoDBClient), infrastructure/resilience/retry/factory.py:54 (same cast), packages/access/sync/adapters/aws_identity_center.py:1226 (role_arn=..., cast to types_boto3_identitystore IdentityStoreClient). Nobody passes session_config/client_config. Tests: tests/integration/infrastructure/idempotency/conftest.py:80,:99 (moto), tests/unit/integrations/aws/test_aws_client.py:57,:103, tests/unit/integrations/aws/test_dynamodb_local_endpoint.py:87.
- classify_aws_error callers: infrastructure/idempotency/dynamodb.py:16, infrastructure/resilience/retry/dynamodb_store.py:17, infrastructure/storage/service.py:22, packages/access/sync/adapters/aws_identity_center.py:27. (infrastructure/operations/classifiers.py defines a DIFFERENT classify_aws_error returning OperationResult; untouched, out of scope.)
- Private botocore access: client.py:8-11 imports DeferredRefreshableCredentials/create_assume_role_refresher; client.py:78 writes session._session._credentials. No other hit under app/integrations or app/infrastructure.
- Settings today: integrations/aws/settings.py (AWSSettings, BaseSettings, lru_cache provider) already has AWS_REGION, AWS_ENDPOINT_URL, RETRY_*, *_TIMEOUT_SECONDS, NOT_FOUND/UNAUTHORIZED/TRANSIENT_CODES with AWS_-prefixed aliases; client.py:15 reads infrastructure/configuration/integrations/aws.py instead, so every getattr(settings, "RETRY_MODE", ...) at client.py:40-44 and :104-123 silently takes the fallback. AWS_ENDPOINT_URL is read only by integrations/aws/shield.py:85 (zero production consumers; deleted in 25.2.6) and its tests; the infrastructure twin ENDPOINT_URL has no reader.
- Endpoint gate: client.py:64-66 (ENVIRONMENT in local/dev/ci -> http://dynamodb-local:8000, via infrastructure.configuration.app); twins at integrations/aws/dynamodb.py:21 and modules/aws/aws_access_requests.py:19 stay for 25.2.5. Environment sources: .devcontainer/docker-compose.yml:30 ENVIRONMENT=dev (+ fake AWS keys); .github/workflows/ci_code.yml:62 ENVIRONMENT=ci with NO dynamodb-local service (tests use moto); terraform/templates/sre-bot.json.tpl:58 sets ENVIRONMENT only. botocore 1.42.54 natively honours AWS_ENDPOINT_URL_DYNAMODB (botocore/configprovider.py:1017).
- Typing facts (verified with `uv run --isolated --locked mypy`): types-boto3 1.43.63 overloads Session.client per Literal and returns types_boto3_<svc>.client.<Svc>Client for dynamodb/identitystore/sso-admin/...; "sts" resolves to Any because pyproject.toml:187 extras omit sts. The types-boto3 DynamoDBClient does NOT satisfy storage/service.py's local Protocol (Unpack kwargs, TypedDict returns), so that cast cannot merely be deleted. This devcontainer's .venv carries a stray boto3-stubs==1.42.54 (uv sync --dry-run would uninstall it) that overwrote the types-boto3 files: local mypy reveals Any for every client until repaired.
- Retry semantics (verified): Config(retries={"mode":"standard","max_attempts":N}) -> total_max_attempts N+1; max_attempts=0 -> exactly one attempt. RETRY_MAX_ATTEMPTS keeps this "retries after the initial call" meaning (human decision).
- RoleSessionName: no sts:RoleSessionName condition anywhere in terraform/; default becomes "sre-bot" (human decision).
- Guard baselines: client.py stays in bin/baselines/sdk_typing_antipatterns.txt because execute_aws_api_call remains until 25.2.6 (regex at bin/check_sdk_typing.py:33-37); vendor_package_contract.txt lists no client.py entry and the new code must keep it that way (no OperationResult reference in client.py/settings.py).
- Legacy helpers stay byte-identical: handle_aws_api_errors, assume_role_session, get_aws_service_client, execute_aws_api_call, paginator (client.py:137-333) and the module constants SYSTEM_ADMIN_PERMISSIONS, VIEW_ONLY_PERMISSIONS, AWS_REGION, THROTTLING_ERRS, RESOURCE_NOT_FOUND_ERRS (AWS_REGION is imported by tests/integration/infrastructure/storage/conftest.py:10, tests/integration/infrastructure/idempotency/conftest.py:13, tests/integration/integrations/aws/test_identity_store_conformance.py:23).

SIZE ESTIMATE / GATE: 9 production files (client.py ~200 changed LOC incl. ten overloads, settings.py ~+50/-3, storage/service.py ~-12/+3, idempotency/factory.py -4, retry/factory.py -2, packages/access adapter -1, shield.py 1 line, pyproject.toml 1 line + regenerated uv.lock, .devcontainer/docker-compose.yml 1 line) ~280 production LOC, two subsystems (integrations/aws + its infrastructure callers; the compose line is config). The cast removals are the mechanical consequence of the typed factory, not an independent refactor. Under the single-PR gate; no decomposition.

STEPS
0. Pre-flight (dev environment only, no repo change): from app/ run `uv sync --locked --reinstall-package types-boto3` so local mypy resolves the types-boto3 overloads like CI does. Re-run `uv run mypy` on a scratch reveal_type(boto3.Session().client("dynamodb")) to confirm DynamoDBClient before touching code.
1. app/pyproject.toml:187 add `sts` to the types-boto3 extras (alphabetical: ...,securityhub,sqs,sso-admin,sts); run `cd app && uv lock` and `uv sync --locked` so uv.lock and the venv carry types-boto3-sts. (AC#1, AC#2)
2. app/integrations/aws/settings.py (single AWS settings home): rewrite the module docstring (it currently says feature fields do not belong here). Keep BaseSettings + the same model_config (integrations must not import infrastructure.configuration.base). Remove AWS_ENDPOINT_URL. Add DYNAMODB_ENDPOINT_URL: str | None = Field(default=None, alias="AWS_ENDPOINT_URL_DYNAMODB", description=...) and TRANSIENT_RETRY_AFTER_SECONDS: int = Field(default=60, alias="AWS_TRANSIENT_RETRY_AFTER_SECONDS"). Add, with identical names and aliases to infrastructure/configuration/integrations/aws.py:35-41: SYSTEM_ADMIN_PERMISSIONS (AWS_SSO_SYSTEM_ADMIN_PERMISSIONS), VIEW_ONLY_PERMISSIONS (AWS_SSO_VIEW_ONLY_PERMISSIONS), AUDIT_ROLE_ARN (AWS_AUDIT_ACCOUNT_ROLE_ARN), ORG_ROLE_ARN (AWS_ORG_ACCOUNT_ROLE_ARN), LOGGING_ROLE_ARN (AWS_LOGGING_ACCOUNT_ROLE_ARN), INSTANCE_ID (AWS_SSO_INSTANCE_ID), INSTANCE_ARN (AWS_SSO_INSTANCE_ARN), all `str = Field(default="", alias=...)`, plus the SERVICE_ROLE_MAP property with the exact mapping of aws.py:62-71. Document RETRY_MAX_ATTEMPTS as "retries after the initial attempt (botocore max_attempts)". infrastructure/configuration/** is not touched. (AC#3, AC#4, AC#5)
3. app/integrations/aws/client.py, factory section (lines 1-88 replaced):
   - Imports: drop the `# type: ignore` on boto3/botocore imports; drop botocore.credentials imports; drop infrastructure.configuration.app; under `if TYPE_CHECKING:` import DynamoDBClient, IdentityStoreClient, OrganizationsClient, SSOAdminClient, CostExplorerClient, ConfigServiceClient, GuardDutyClient, SecurityHubClient, LambdaClient, STSClient from types_boto3_<svc>.client (module names: dynamodb, identitystore, organizations, sso_admin, ce, config, guardduty, securityhub, lambda, sts). Import get_aws_settings from integrations.aws.settings.
   - `type AwsServiceName = Literal["dynamodb","identitystore","organizations","sso-admin","ce","config","guardduty","securityhub","lambda","sts"]`.
   - Ten @overload signatures `get_aws_client(service_name: Literal["<svc>"], *, role_arn: str | None = None, session_name: str = "sre-bot", retries: bool = True) -> <Svc>Client`, then the implementation `def get_aws_client(service_name: AwsServiceName, *, role_arn=None, session_name="sre-bot", retries=True) -> Any` (the only Any: session.client on a Literal union cannot match the stub overloads). session_config/client_config kwargs are gone.
   - `_build_config(settings, *, retries: bool) -> Config`: Config(retries={"mode": settings.RETRY_MODE, "max_attempts": settings.RETRY_MAX_ATTEMPTS if retries else 0}, connect_timeout=settings.CONNECT_TIMEOUT_SECONDS, read_timeout=settings.READ_TIMEOUT_SECONDS). Settings are read via get_aws_settings() at call time (not at import) so overrides and cache_clear take effect.
   - `_assume_role_credentials(sts: STSClient, role_arn: str, session_name: str)` calls sts.assume_role(RoleArn=..., RoleSessionName=...) and returns response["Credentials"]; `_session_for(settings, role_arn, session_name)` builds boto3.Session(region_name=settings.AWS_REGION) and, when role_arn is set, builds an sts client from it with the retrying config, then returns boto3.Session(aws_access_key_id=..., aws_secret_access_key=..., aws_session_token=..., region_name=settings.AWS_REGION) from the returned credentials. Public API only; assume_role failures raise (clients raise, adapters classify). Clients are built per call and never cached, so no refresh logic.
   - Endpoint gate once: `endpoint_url = settings.DYNAMODB_ENDPOINT_URL if service_name == "dynamodb" else None`; pass region_name, config and endpoint_url to session.client.
   - classify_aws_error: replace every getattr(...) default with the settings fields (NOT_FOUND_CODES, UNAUTHORIZED_CODES, TRANSIENT_CODES, TRANSIENT_RETRY_AFTER_SECONDS) read from get_aws_settings(); mapping order and the ConditionalCheckFailedException / BotoCoreError / propagate branches unchanged.
   - Legacy block (kept, clearly delimited with a comment naming its removal trigger "deleted with the mirror modules"): `from infrastructure.configuration.integrations.aws import get_aws_settings as _get_legacy_aws_settings` feeding the five module constants, then handle_aws_api_errors, assume_role_session, get_aws_service_client, execute_aws_api_call, paginator byte-identical. (AC#1, AC#2, AC#3, AC#4, AC#6)
4. app/infrastructure/storage/service.py: delete the local DynamoDBClient Protocol (lines 30-37); add `if TYPE_CHECKING: from types_boto3_dynamodb.client import DynamoDBClient`; DynamoDBStorageService.__init__ keeps the name DynamoDBClient as its annotation; get_storage_service passes get_aws_client("dynamodb") without cast; drop `cast`/`Protocol` imports if unused; update the docstring mention. (AC#1)
5. app/infrastructure/idempotency/factory.py:29,:41 and app/infrastructure/resilience/retry/factory.py:54: remove the casts; drop the now-unused TYPE_CHECKING DynamoDBClient import and `cast` import where nothing else uses them. (AC#1)
6. app/packages/access/sync/adapters/aws_identity_center.py:1226: drop the cast only (the TYPE_CHECKING alias at :47 stays for the __init__ annotation at :99); nothing else in packages/access changes. (AC#1)
7. app/integrations/aws/shield.py:85: pass endpoint_url=None (field removed); no other shield change. (settings consolidation)
8. .devcontainer/docker-compose.yml app service environment: add `AWS_ENDPOINT_URL_DYNAMODB: 'http://dynamodb-local:8000'` next to ENVIRONMENT. CI and terraform untouched (CI uses moto; production must not carry it). (new endpoint AC)
9. Tests (see matrix), then gates from app/: `uv run ruff check .`, `uv run mypy . --exclude '(?:^|/)\.venv(?:/|$)'`, `uv run pytest tests --ignore=tests/smoke`; plus evidence greps: `rg -n "DeferredRefreshableCredentials|create_assume_role_refresher|_session\._credentials|botocore\.credentials" app/integrations` = 0 hits; `rg -n "cast\(" app/infrastructure/storage/service.py app/infrastructure/idempotency/factory.py app/infrastructure/resilience/retry/factory.py` = 0 hits on get_aws_client lines; `make check-sdk-typing` and `make check-vendor-package-contract` still OK (client.py remains baselined for execute_aws_api_call; no new entries).

TEST MATRIX (all under app/tests/unit/integrations/aws/, autouse conftest clears AWS_* env and the settings cache; tests construct REAL boto3 clients with region only, no network, and assert client.meta.config / client.meta.endpoint_url; botocore.stub.Stubber for STS)
- test_aws_client_factory_config.py (new): default Config for each variant (retries=True -> retries {"mode":"standard","total_max_attempts":4}, connect/read 10; retries=False -> total_max_attempts 1, same timeouts); env overrides take effect (AWS_RETRY_MODE=adaptive, AWS_RETRY_MAX_ATTEMPTS=5, AWS_CONNECT_TIMEOUT_SECONDS=2, AWS_READ_TIMEOUT_SECONDS=7, AWS_REGION=eu-west-1 via monkeypatch.setenv + cache_clear, asserting the resulting client meta); endpoint gate: AWS_ENDPOINT_URL_DYNAMODB set -> dynamodb client meta.endpoint_url equals it; unset -> regional AWS endpoint; set but service identitystore -> regional endpoint (gate is dynamodb-only); every Literal service name builds a client of the expected meta.service_model.service_name (parametrized over the ten names; sts included); clients are distinct objects across two calls (no caching); a typed-usage assertion file section relies on mypy (AC#1 evidence is the mypy gate on the three callers, not a runtime test). [AC#1, AC#3, new endpoint AC]
- test_aws_client_assume_role.py (new): _assume_role_credentials with Stubber on a real sts client: add_response("assume_role", canned Credentials, expected_params={"RoleArn": ..., "RoleSessionName": "sre-bot"}) returns the credentials; custom session_name is forwarded; Stubber add_client_error("assume_role", "AccessDenied") propagates ClientError (no swallow); get_aws_client("identitystore", role_arn=...) wiring test with a recording fake for boto3.Session that delegates to the real Session: asserts the second Session is constructed with the assumed key/secret/token and region, that assume_role happened before the client was returned (eager), and that no botocore private attribute is set (the recorded session objects have no injected _credentials). [AC#2]
- test_aws_client_classify_error.py (new): parametrized mapped families: ResourceNotFoundException/NoSuchEntity -> NOT_FOUND; AccessDeniedException/ExpiredToken -> UNAUTHORIZED; ThrottlingException/ServiceUnavailable -> TRANSIENT_ERROR with retry_after 60; retry_after follows AWS_TRANSIENT_RETRY_AFTER_SECONDS=15 override; ConditionalCheckFailedException -> PERMANENT_ERROR, retry_after None; BotoCoreError subclass (EndpointConnectionError) -> TRANSIENT_ERROR, error_code class name, retry_after None; catalogues are read from settings (AWS_NOT_FOUND_CODES override maps a custom code); unmapped ClientError ("ValidationException") propagates; ClientError with empty Code propagates; KeyError propagates. [AC#4]
- test_aws_settings_fields.py (new, replaces test_settings.py): transport defaults and env aliases (region, retry mode/attempts, timeouts, DYNAMODB_ENDPOINT_URL via AWS_ENDPOINT_URL_DYNAMODB, TRANSIENT_RETRY_AFTER_SECONDS via AWS_TRANSIENT_RETRY_AFTER_SECONDS); AWS_ENDPOINT_URL is no longer a field; each of the seven feature fields defaults to "" and reads its exact env alias (parametrized on the (field, alias) pairs listed in step 2); SERVICE_ROLE_MAP composes the three role ARNs into the eight keys exactly as the infrastructure module does (equality against infrastructure.configuration.integrations.aws.AwsSettings built from the same env, which pins parity until 25.2.6 deletes it); catalogues overridable at construction; provider singleton and cache_clear behaviour. [AC#5]
- Deleted once superseded: tests/unit/integrations/aws/test_aws_client.py, tests/unit/integrations/aws/test_settings.py; in test_dynamodb_local_endpoint.py remove only test_integrations_aws_client_dynamodb_endpoint_matrix (lines 50-91; the dynamodb.py and aws_access_requests cases stay for 25.2.5); in test_shield.py drop AWS_ENDPOINT_URL=None from the fixture and delete test_client_passes_endpoint_url_when_set / test_client_passes_none_endpoint_url_when_unset; tests/smoke/integrations/aws/test_shield_smoke.py:35 drop the AWS_ENDPOINT_URL kwarg (see doubts).
- Regression: full unit suite, in particular tests/unit/infrastructure/storage, tests/unit/infrastructure/idempotency, tests/unit/infrastructure/resilience, tests/unit/packages/access, tests/integrations/aws/* (legacy mirrors) and tests/integration/** (moto) must stay green unchanged. [AC#6]

AC TRACEABILITY: AC#1 -> steps 0,1,3,4,5,6 -> mypy gate + factory_config Literal parametrization; AC#2 -> step 3 -> assume_role tests + private-attribute grep; AC#3 -> steps 2,3 -> factory_config tests; AC#4 -> steps 2,3 -> classify_error tests; AC#5 -> step 2 -> settings_fields tests; AC#6 -> legacy block untouched -> existing tests/integrations/aws/* and tests/integration/**; new endpoint AC -> steps 2,3,8 -> factory_config endpoint tests.

ASSUMPTIONS AND DOUBTS
- A1: types-boto3 1.43.63 publishes a matching types-boto3-sts wheel; verify `uv lock` resolves and `uv run mypy` reveals STSClient after step 1.
- A2: session.client(...) with a Literal-union service_name needs one Any inside the implementation; if mypy accepts a narrower form (e.g. per-overload dispatch), prefer it. Verify with the mypy gate.
- A3: Storage unit tests pass MagicMock into DynamoDBStorageService; replacing the Protocol with the stub type is invisible to them because tests are excluded from mypy (pyproject.toml exclude ^tests/). Verify by running tests/unit/infrastructure/storage.
- A4: moto intercepts clients built without endpoint_url in CI exactly as it does today with the forced endpoint (tests/integration/infrastructure/idempotency/conftest.py:80,:99); verify by running tests/integration locally with AWS_ENDPOINT_URL_DYNAMODB unset.
- A5: Developers must recreate the devcontainer (docker compose up) to receive the new compose variable; until then a dev container with ENVIRONMENT=dev but no variable builds dynamodb clients against real AWS with the fake compose credentials (calls fail with UnrecognizedClientException rather than touching real data). Record in the PR description.
- D1: With AWS_ENDPOINT_URL removed, tests/smoke/integrations/aws/test_shield_smoke.py can no longer target a local endpoint; the file is deleted in 25.2.6 and is never run by default. Plan removes the kwarg so the fixture does not silently ignore it; reviewer may prefer to skip-mark the smoke class instead.
- D2: The infrastructure AwsSettings.THROTTLING_ERRS / RESOURCE_NOT_FOUND_ERRS are not added to AWSSettings (human decision: legacy constants keep the infrastructure import until 25.2.6).
- D3: get_aws_settings() is called per factory/classify call rather than bound at import; lru_cache makes it free and it removes the monkeypatch-the-module-global test pattern. Verify no caller relied on `integrations.aws.client.settings` (rg shows none outside the deleted tests).

BLAST RADIUS AND ROLLBACK
- Every DynamoDB-backed infrastructure service (storage, idempotency, retry store) and packages/access's identitystore adapter construct clients here; a wrong Config, region or endpoint breaks all persistence at once, which the factory tests and the moto integration suites guard. AssumeRole is exercised in production only by packages/access (feature not enabled), so the eager path carries low runtime exposure.
- Behaviour changes to disclose in the PR: (1) retry/timeout settings become effective for the first time (today's getattr fallbacks happen to equal the defaults, so effective values are unchanged unless an AWS_RETRY_*/AWS_*_TIMEOUT_SECONDS variable is already set somewhere; verify env on staging/production before merge); (2) endpoint gate moves from ENVIRONMENT to AWS_ENDPOINT_URL_DYNAMODB; (3) RoleSessionName "sre-bot".
- Single `git revert` restores the previous code, lockfile and compose line together; no data migration, no ordering constraint beyond the compose line landing in the same PR.
<!-- SECTION:PLAN:END -->

## Comments

<!-- COMMENTS:BEGIN -->
created: 2026-09-11 17:31
---
Planning notes 2026-09-11: (1) AC#1 'casts removed' for storage requires replacing infrastructure/storage/service.py's local DynamoDBClient Protocol with the types-boto3 DynamoDBClient under TYPE_CHECKING, because mypy confirms the stub type does not satisfy that Protocol; the AC text stands, this comment records how it is met. (2) RETRY_MAX_ATTEMPTS keeps botocore's max_attempts meaning (retries after the initial call; default 3 -> 4 total attempts, unchanged), retries=False passes max_attempts=0. (3) Legacy constants and helpers keep reading infrastructure/configuration/integrations/aws.py in a delimited block removed by 25.2.6; THROTTLING_ERRS/RESOURCE_NOT_FOUND_ERRS are not added to AWSSettings. (4) Open point for reviewer: tests/smoke/integrations/aws/test_shield_smoke.py loses its local-endpoint routing once AWS_ENDPOINT_URL is removed; plan drops the kwarg, alternative is a skip mark until 25.2.6 deletes the file. (5) Pre-flight for the implementer: this devcontainer's .venv has a stray boto3-stubs==1.42.54 shadowing types-boto3; run 'uv sync --locked --reinstall-package types-boto3' before trusting local mypy.
---
<!-- COMMENTS:END -->
