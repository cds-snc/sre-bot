---
id: TASK-22.4
title: >-
  Migrate directory provider off infrastructure/clients/google_workspace onto
  integrations/google_workspace/google_directory_next
status: In Progress
assignee:
  - '@me'
created_date: '2026-07-29 21:11'
updated_date: '2026-08-07 22:04'
labels:
  - clients
  - phase-3
milestone: m-3
dependencies:
  - TASK-22.3
  - TASK-70
references:
  - decisions/layers.md
  - decisions/outbound-clients.md
  - decisions/sdk-typing.md
  - app/infrastructure/directory/factory.py
  - app/infrastructure/directory/google.py
parent_task_id: TASK-22
priority: high
ordinal: 107000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Slice 4 of TASK-22 (parent). Repoint the Google Workspace directory consumers off the deprecated GoogleWorkspaceClients facade onto integrations/google_workspace/google_directory_next.

Call sites: infrastructure/directory/factory.py:6 (GoogleWorkspaceClients, get_google_workspace_clients) and infrastructure/directory/google.py:7 (GoogleWorkspaceClients). GoogleDirectoryProvider calls self._directory.<method> at 11 sites (google.py lines 287-705): health_check, get_user, list_users, list_members, get_batch_group_members, get_group, add_member, remove_member, has_member, list_groups, list_user_groups. google_directory_next.py covers most as MODULE-LEVEL OperationResult functions. VERIFY GAP: confirm a health_check equivalent exists in google_directory_next/google_service_next; if absent, add a minimal OperationResult health_check there (behavior-parity with the deprecated one) as part of this slice. Rework factory.py so GoogleDirectoryProvider no longer receives a GoogleWorkspaceClients facade. Preserve all OperationResult outcomes and payload normalization exactly. Wire to _next as-is (no rename = TASK-23; no raise/classify = TASK-25).

Test migration: relocate app/tests/integrations/google_workspace/test_google_directory_next.py and test_google_service_next.py to app/tests/unit/integrations/google_workspace/; keep directory provider tests in tests/unit/infrastructure/directory/, updating mock target paths. Legacy tests/integrations/ count must drop.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 infrastructure/directory/factory.py and google.py no longer import infrastructure.clients.google_workspace; GoogleDirectoryProvider is backed by a Google Directory adapter that calls the discovery Resource directly (built by a factory with cache_discovery=False, static_discovery=True that RAISES, and whose return is annotated with the google-api-python-client-stubs-provided Resource type, e.g. AdminDirectoryResource, imported under TYPE_CHECKING per decisions/sdk-typing.md item 3) and translates responses into typed domain dataclasses via classify_google_error - the execute_google_api_call dispatcher and the get_google_api_command_parameters docstring scraper are NOT on the directory path
- [x] #2 A health_check equivalent returning OperationResult exists in integrations/google_workspace and is used by GoogleDirectoryProvider with behavior parity
- [x] #3 All tests/unit/infrastructure/directory/ tests pass behavior-neutral (identical OperationResult outcomes + payload normalization across the 11 provider calls); classify_google_error has coverage under tests/unit/integrations/google_workspace/; google_directory_next/google_service_next are left for TASK-23 deletion; touched vendor tests land under tests/unit (legacy tests/integrations/ count does not grow)
- [x] #4 get_admin_directory_service is invoked per-operation with that operation's existing, narrowly-scoped OAuth scope list (e.g. admin.directory.user.readonly for get_user/list_users, admin.directory.group.member for add_member/remove_member, etc.) rather than a broader unioned scope set, preserving today's per-call least-privilege delegated-credential behavior; covered by a unit test asserting the exact scopes passed to the injected get_service callable per method
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 Behavior-neutral: directory provider OperationResult outcomes identical; PR references decisions/layers.md, decisions/outbound-clients.md, and decisions/sdk-typing.md
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
REASSESSED 2026-08-07 (fresh grounding pass; corrects the 2026-07-31/07-31-second-pass plan's central assumption about which code path is actually live). Design direction (typed google-api-python-client-stubs Resource + classify_google_error, no facade) is UNCHANGED; the WHICH-FILES-ARE-INVOLVED grounding is substantially corrected below.

GROUNDING CORRECTIONS (re-verified against current code, not the stale prose):
- MAJOR CORRECTION: the existing plan's claim "today those [11 provider calls] resolve through google_directory_next -> execute_google_api_call (google_service_next), which walks the discovery tree by string and scrapes method docstrings" is WRONG for the live path. GoogleDirectoryProvider.__init__ (infrastructure/directory/google.py) receives google_clients.directory, which is infrastructure/clients/google_workspace/facade.py::GoogleWorkspaceClients.directory = DirectoryClient(...) (infrastructure/clients/google_workspace/directory.py). DirectoryClient already calls service.<resource>().<method>(...).execute() DIRECTLY (e.g. service.users().get(userKey=...), service.groups().list(customer=..., **kwargs) with manual list_next pagination) wrapped in infrastructure/clients/google_workspace/executor.py::execute_google_api_call — a 3-arg (operation_name, api_callable, max_retries) retry+error wrapper that is NOT the docstring-scraping dispatcher. There is a THIRD, wholly separate legacy implementation — integrations/google_workspace/google_directory.py + google_service.py — which DOES contain the real docstring scraper (get_google_api_command_parameters, confirmed at google_service.py:238) and the 4-arg execute_google_api_call dispatcher (google_service.py:165), but it is dead weight on this call path: GoogleDirectoryProvider never touches it. AC#1's assertion ("the execute_google_api_call dispatcher and the get_google_api_command_parameters docstring scraper are NOT on the directory path") is therefore ALREADY TRUE TODAY, not something this task achieves — it was already true before this task starts, because the provider never used that module. What this task actually removes is the STANDING FACADE CLASS (DirectoryClient / GoogleWorkspaceClients, exposing SDK-shaped passthrough methods returning OperationResult) — the "no standing wrapper class" rule in decisions/outbound-clients.md — plus adds the stub-typed annotation and a real classify_google_error (today's executor.py has none: every error becomes OperationResult.permanent_error with a generic error_code "GOOGLE_API_ERROR_{status}"/"GOOGLE_API_ERROR", no NOT_FOUND/UNAUTHORIZED/TRANSIENT_ERROR distinction at all).
- CALL SITE COUNT CONFIRMED: infrastructure/directory/google.py has 12 self._directory.<method>(...) call sites for 11 distinct methods (list_groups is called twice, at ~649 alias-aware / ~652 with query — same method, mirroring the get_group_membership_id double-call-site pattern from TASK-22.3): health_check (warmup only — the provider's own health_check() override at line ~295 is a local no-op returning success without touching self._directory), get_user, list_users, list_members, get_batch_group_members, get_group, add_member, remove_member, has_member, list_groups (x2), list_user_groups. All 11 are real Admin SDK Directory API operations backed by DirectoryClient methods that already call the discovery Resource directly — confirmed by reading each: get_user->users().get, list_users->users().list (+list_next), list_groups->groups().list (+list_next), get_group->groups().get, add_member->members().insert, remove_member->members().delete, has_member->members().hasMember, list_user_groups->groups().list(userKey=...) (+list_next), health_check->customers().get, get_batch_group_members->members().list per group via service.new_batch_http_request() batching (infrastructure/clients/google_workspace/batch_executor.py — a real googleapiclient BatchHttpRequest mechanism, not a homemade dispatcher; must be preserved, not deleted, as it is legitimate SDK usage).
- HEALTH_CHECK GAP CLOSED: the existing plan's "VERIFY GAP: confirm a health_check equivalent exists... if absent, add one" is resolved — DirectoryClient.health_check() (line ~291) ALREADY EXISTS today (calls service.customers().get(customerKey=...)), so AC#2 is about relocating/preserving existing behavior into the new integrations factory/adapter, not adding a net-new capability.
- STATIC/CACHE DISCOVERY DOUBT RESOLVED: infrastructure/clients/google_workspace/session_provider.py::SessionProvider.get_service() already builds with cache_discovery=False, static_discovery=True (confirmed, line ~84-89) — no change needed to those flags; only the return-type annotation and facade-class removal are new work. (integrations/google_workspace/google_service_next.py::get_google_service, by contrast, uses static_discovery=False — but that module is NOT on this path and is NOT touched by this task.)
- STUB TYPE RESOLUTION VERIFIED (resolves doubt (d) from the prior plan definitively, not just by inspection): ran a real mypy check against this repo's actual dev environment (types installed: google-api-python-client-stubs 1.39.0). `from googleapiclient._apis.admin_directory_v1 import AdminDirectoryResource` (and CalendarResource from calendar_v3, MeetResource from meet_v2) resolve with "Success: no issues found" under `if TYPE_CHECKING:` import + string-literal annotation. The stub package installs to .venv/lib/.../site-packages/googleapiclient-stubs/_apis/ (a PEP 561 stub-only package: real folder name has a "-stubs" suffix, invisible to normal runtime import — `import googleapiclient._apis...` genuinely raises ModuleNotFoundError at runtime, confirmed — so the TYPE_CHECKING guard is not just style, it is load-bearing).
- NEW FINDING, MATERIAL TO ADAPTER SHAPE (not addressed in either prior plan pass): unlike AWS/DynamoDB (one long-lived client, built once), the CURRENT Google Directory facade builds a FRESH discovery Resource on every single method call, each with a DIFFERENT, narrowly-scoped OAuth scope list matching only that operation's need (confirmed per-method: get_user/list_users -> admin.directory.user.readonly; list_groups/get_group -> admin.directory.group.readonly; add_member/remove_member -> admin.directory.group.member; has_member -> admin.directory.group.member.readonly; list_user_groups -> admin.directory.group.readonly; health_check -> admin.directory.customer.readonly; get_batch_group_members -> admin.directory.group.readonly). This is deliberate least-privilege scoping of the delegated service-account credentials per call, not incidental. A single Resource built once with a UNION of all scopes (mirroring the DynamoDB "build once, adapter holds it" shape naively) would be a real, security-relevant behavior change (broader token scope than today, even though a server-side service account may not visibly differ to callers) and must NOT be done silently. TARGET SHAPE below preserves the per-call scoping by injecting the FACTORY CALLABLE (scopes -> Resource), not a pre-built Resource, into the provider — the provider calls it once per method with that method's own scope list, exactly mirroring today's call-site shape.
- NO DOWNSTREAM ERROR-CODE COUPLING: grepped app/packages/access/** (the only consumers of DirectoryProvider) and infrastructure/directory/google.py itself — no caller branches on the current generic "GOOGLE_API_ERROR_*" error_code strings (google.py's own _typed_error passes them through opaquely; its own re-derived codes like DIRECTORY_USER_MAPPING_INVALID are post-processing on top, unaffected). This means classify_google_error MAY introduce genuinely finer-grained OperationStatus (NOT_FOUND/UNAUTHORIZED/TRANSIENT_ERROR per HttpError.resp.status) without breaking any existing branch — a real behavior *refinement*, not neutral in the strictest sense, but safe. Flagged for human confirmation since "preserve OperationResult outcomes exactly" (AC#3) should be read as "is_success/data shape unchanged", not "the previously-generic error_code string is bit-for-bit preserved."
- DOMAIN DATACLASS TRANSLATION ALREADY DONE: sdk-typing.md item 3's "translate into typed domain dataclasses" requirement is ALREADY satisfied — GoogleDirectoryProvider's own _build_directory_user/_build_directory_group/_build_directory_member/_typed_error methods already do this today, unchanged by this task. This narrows the real diff to: replace self._directory.<method>(dict-returning-OperationResult) with a direct, typed, classified Resource call — the dataclass-building code below each call site is untouched.
- GUARDRAIL BASELINES CONFIRMED: app/bin/baselines/deprecated_infra_client_imports.txt lines 10-11 list exactly infrastructure/directory/factory.py and infrastructure/directory/google.py — these are the two files whose removal from that baseline is this task's measurable "ratchet down" signal (same mechanism TASK-22.3 used). app/bin/baselines/sdk_typing_antipatterns.txt only lists files under app/integrations/google_workspace/ (the dispatcher/scraper pattern, scoped by TASK-70's checker to integrations/ only) — it does NOT list the infrastructure/clients/google_workspace facade, so removing the facade does not shrink that baseline; no action needed there.
- ORPHANED CODE, OUT OF SCOPE: infrastructure/clients/google_workspace/{directory.py (DirectoryClient), and its slice of executor.py/session_provider.py/batch_executor.py} become unused-by-DirectoryProvider after this task, but facade.py's GoogleWorkspaceClients still constructs DirectoryClient (its other services — drive/docs/sheets/gmail — remain wired through the same facade, out of this task's scope). Do NOT delete DirectoryClient, facade.py's directory composition, or tests/unit/infrastructure/clients/google_workspace/test_directory.py in this task — that is a later cleanup slice (mirrors how TASK-22.3 left AWSClients.identitystore composition for TASK-22.5). executor.py/session_provider.py/batch_executor.py stay fully alive (drive/docs/sheets/gmail still use them).

TARGET SHAPE (decisions/outbound-clients.md Path A + sdk-typing.md item 3 — this is a portable-capability Protocol per decisions/layers.md, so the adapter lives directly in infrastructure/directory/, not packages/<feature>/adapters/):
1. New module integrations/google_workspace/client.py (the vendor package's two sanctioned exports, per outbound-clients.md): 
   - get_admin_directory_service(scopes: list[str], delegated_user_email: str | None = None) -> "AdminDirectoryResource": builds credentials from get_google_workspace_settings() (GCP_SRE_SERVICE_ACCOUNT_KEY_FILE, SRE_BOT_EMAIL as default delegated_user_email), calls build("admin", "directory_v1", credentials=creds, cache_discovery=False, static_discovery=True), annotated with AdminDirectoryResource (from googleapiclient._apis.admin_directory_v1, imported under TYPE_CHECKING) per sdk-typing.md item 3. RAISES on credential/build failure (ValueError/JSONDecodeError) — no OperationResult, no retry loop (mirrors session_provider.get_service()'s existing body almost verbatim, just relocated and typed).
   - classify_google_error(exc: Exception) -> tuple[OperationStatus, str | None, int | None]: maps HttpError.resp.status (404 -> NOT_FOUND; 401/403 -> UNAUTHORIZED; 429/500/502/503/504 -> TRANSIENT_ERROR, honoring a Retry-After response header via exc.resp.get("retry-after") if present) onto the closed OperationStatus set, mirroring classify_aws_error's shape (integrations/aws/client.py) as the same-repo precedent; unmapped HttpError statuses and non-HttpError exceptions propagate (raise), per outbound-clients.md's "unexpected exceptions are not classified."
2. infrastructure/directory/google.py — GoogleDirectoryProvider becomes the adapter itself (Path A: Protocol implementation IS the boundary, no intermediate facade):
   - Constructor changes from (google_clients: GoogleWorkspaceClients, directory_settings: DirectorySettings) to (get_service: Callable[[list[str]], "AdminDirectoryResource"], directory_settings: DirectorySettings, customer_id: str) — injecting the scoped-factory CALLABLE (not a pre-built Resource) to preserve today's per-call least-privilege scoping exactly; testable by injecting a fake callable/fake Resource.
   - Add private helpers _call(fn) -> OperationResult (try/except HttpError -> classify_google_error; success wraps fn()'s return) and _paginate(list_call, list_next_call, response_key) -> OperationResult (list_next pagination idiom, same as today's DirectoryClient loops, just executed inline).
   - Convert each of the 11 methods: call self._get_service([<method's existing scope(s)>]) then the same service.<resource>().<method>(...) shape DirectoryClient already uses today, through _call/_paginate. get_batch_group_members keeps using service.new_batch_http_request() (batch_executor.py's execute_batch_request helper stays reusable as-is, or is inlined — implementer's choice, no behavior change either way).
   - Remove the "google_clients: GoogleWorkspaceClients facade injected by the factory" docstring/class-doc references; drop the infrastructure.clients.google_workspace import entirely.
3. infrastructure/directory/factory.py — build_google_directory_provider(directory_settings) resolves get_google_workspace_settings() itself, builds a partial(get_admin_directory_service, delegated_user_email=settings.SRE_BOT_EMAIL or None) as the injected get_service callable, and passes customer_id=settings.GOOGLE_WORKSPACE_CUSTOMER_ID. Drops the google_clients: GoogleWorkspaceClients parameter and the get_google_workspace_clients() call in get_directory_provider() entirely.
4. Test rework (same mechanical transformation TASK-22.3 already established for AWS — OperationResult-returning fake -> raw-dict/HttpError-raising fake):
   - tests/unit/infrastructure/directory/test_google.py: FakeDirectoryClient-style mocks (currently mock_google_clients.directory.<method>.return_value = OperationResult...) rebuilt as a fake scoped-Resource-returning callable whose chained resource().method().execute() either returns a raw dict/list or raises googleapiclient.errors.HttpError(resp, content) for failure-path tests. Keep existing test NAMES/assertions on provider behavior (result.is_success, result.data, result.status) unchanged where they test the provider's own dataclass-mapping logic; only the fake's construction and failure-injection mechanism change.
   - tests/unit/infrastructure/directory/test_factory.py: update the 3 tests that currently pass google_clients=MagicMock() — construct via the new (get_service, directory_settings, customer_id) signature instead, or monkeypatch get_google_workspace_settings()/get_admin_directory_service.
   - New tests/unit/integrations/google_workspace/test_client.py: classify_google_error family coverage (404->NOT_FOUND, 401/403->UNAUTHORIZED, 429/5xx->TRANSIENT_ERROR + retry_after, unmapped propagates) + get_admin_directory_service builds with cache_discovery=False/static_discovery=True and resolves the AdminDirectoryResource-annotated return under mypy.
   - No legacy tests/integrations/ count impact — this task does not touch integrations/google_workspace/{google_directory.py, google_directory_next.py, google_service.py, google_service_next.py} at all (confirmed above they are not on this call path); those stay exactly as-is for TASK-23/25 to resolve.

AC-TO-STEP: AC#1 -> Steps 1-3 (grep infrastructure.clients.google_workspace in factory.py/google.py returns nothing; adapter calls the discovery Resource directly via the injected per-call scoped factory + classify_google_error; no dispatcher/scraper on the directory path — true both before and after this task, now correctly attributed). AC#2 -> Step 2 (health_check relocated with parity — it already existed, this task moves and preserves it, does not invent it). AC#3 -> Steps 1, 4 (classify_google_error coverage under tests/unit/integrations/google_workspace/; google_directory_next/google_service_next untouched; no growth in tests/integrations/ count since nothing there is touched). New AC needed (human review, mirrors TASK-22.3's AC#5 role-assumption addition): per-call OAuth scope minimization must be preserved — each of the 11 operations requests only its own existing scope list from get_admin_directory_service, not a broader/unioned scope set — covered by a unit test asserting the exact scopes list passed to the injected get_service callable per method.

TEST MATRIX: happy path (each of 11 ops returns the same shape as today — dict/list payloads matching current DirectoryClient outputs, including paginated aggregation for list_users/list_groups/list_members-equivalent list_user_groups); classify (HttpError status -> expected OperationStatus/error_code/retry_after per new classify_google_error families; unmapped HttpError status and non-HttpError exceptions propagate); scope minimization (each method's call to the injected get_service callable carries exactly that method's pre-existing scope list, not a superset); health_check parity (customers().get(customerKey=...) preserved); batch (get_batch_group_members still uses new_batch_http_request via batch_executor.execute_batch_request, unchanged shape); typing (mypy resolves get_admin_directory_service's AdminDirectoryResource-annotated return and the adapter's chained service.users()/groups()/members() calls with no Any leakage). Commands: cd app && uv run pytest tests/unit/infrastructure/directory tests/unit/integrations/google_workspace -v; then tests --ignore=tests/smoke; then ruff check . and mypy . --exclude '(?:^|/)\.venv(?:/|$)'; then make check-sdk-typing and grep infrastructure.clients.google_workspace app/infrastructure/directory/{factory,google}.py (expect no matches) to confirm the deprecated-import baseline can shrink by those 2 lines.

BLAST RADIUS / ROLLBACK: contained to infrastructure/directory/{factory.py,google.py}, a new integrations/google_workspace/client.py, and 2 test files (+1 new test file). infrastructure/clients/google_workspace/{directory.py,executor.py,session_provider.py,batch_executor.py,facade.py} and their existing tests are untouched (still serving drive/docs/sheets/gmail) — DirectoryClient becomes orphaned-but-not-deleted. The DirectoryProvider Protocol and its packages/access/** consumers are untouched (only the injected-collaborator shape inside GoogleDirectoryProvider changes, not its public Protocol methods/signatures). Single git revert restores the facade-backed provider. Depends on TASK-70 (Done — google-api-python-client-stubs now installed and mypy-verified) and TASK-22.3 (in-progress but its AC/notes are complete; independent subsystem regardless).

SIZE GATE: one capability surface (directory provider) + one new integrations module (factory function + classify function) + factory.py wiring — single subsystem, no packages/ changes, no cross-cutting refactor. Production diff: 2 files edited (factory.py trivial ~10 line change; google.py's 11 call sites converted in place, dataclass-building code below each stays untouched) + 1 new ~80-100 line integrations module. Test diff: 2 files reworked (mechanical fake-shape change, same pattern as TASK-22.3) + 1 new classify-coverage test file. VERDICT: fits comfortably in one PR — smaller than TASK-22.3's equivalent slice (no batch-heavy dispatcher rewrite needed since DirectoryClient already calls the Resource directly; this task's job is narrower: delete the facade tier and add typing/classification, not reinvent the call shape).

DOUBTS (verify at impl): (a) exact Retry-After header access on googleapiclient HttpError — confirm exc.resp.get("retry-after") (httplib2.Response is dict-like) returns the right value/format in a live test, or whether Google's Admin SDK ever actually sends it for 429s (may always be None in practice — classify_google_error must handle that gracefully either way); (b) whether get_batch_group_members's batch_executor.execute_batch_request usage should be inlined into google.py or kept as a reused helper import from infrastructure/clients/google_workspace/batch_executor.py — reusing it avoids duplicating BatchHttpRequest wiring but keeps one import from the soon-to-be-orphaned infrastructure/clients tree; recommend copying the ~15-line helper into the new integrations/google_workspace/client.py (or inline in google.py) instead, so infrastructure/directory/google.py has zero infrastructure.clients.google_workspace imports post-migration (cleaner AC#1 grep) — flagged for human confirmation, not mandatory; (c) whether SRE_BOT_EMAIL should be threaded as a per-call default inside get_admin_directory_service (mirroring today's SessionProvider default_delegated_email) or passed explicitly from factory.py's partial() — either preserves identical behavior, pure implementer style choice.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Completed the migration started in a prior session: GoogleDirectoryProvider (infrastructure/directory/google.py) and build_google_directory_provider (infrastructure/directory/factory.py) no longer reference infrastructure.clients.google_workspace; they call the Admin SDK Directory discovery Resource directly via an injected per-call scoped get_service factory. New integrations/google_workspace/client.py provides get_admin_directory_service (raises, cache_discovery=False/static_discovery=True, TYPE_CHECKING-only stub-typed return) and classify_google_error (HttpError status -> NOT_FOUND/UNAUTHORIZED/TRANSIENT_ERROR, unmapped/non-HttpError propagate). health_check/warmup parity preserved (customers().get). Per-call OAuth scope minimization covered by tests/unit/infrastructure/directory/test_google_scoped_service_contract.py (10 cases). classify_google_error covered by tests/unit/integrations/google_workspace/test_client.py. tests/integrations/ legacy count unchanged (33 files, untouched vs main).\n\nFixed two bugs found while finishing the session: (1) the TYPE_CHECKING stub import used a wrong path/name (googleapiclient._apis.admin_directory_v1.AdminDirectoryResource does not exist in google-api-python-client-stubs); corrected to googleapiclient._apis.admin.directory_v1.DirectoryResource, aliased locally as AdminDirectoryResource, across client.py/google.py/factory.py. (2) execute_batch_request called BatchHttpRequest.execute(num_retries=3), but BatchHttpRequest.execute only accepts an optional http argument at runtime/in the stubs -- would have raised TypeError in production; removed the invalid kwarg.\n\nTest evidence: tests/unit/infrastructure/directory + tests/unit/integrations/google_workspace = 71 passed. mypy clean on the three touched files (rm -rf .mypy_cache && uv run mypy .); ruff check . passes. Full uv run pytest tests --ignore=tests/smoke shows 5 pre-existing failures (2 in tests/integrations/aws/test_client_next.py, 3 in tests/modules/webhooks/test_webhooks_aws_sns.py) caused by structlog capture_logs cross-test pollution when running tests/integration and tests/modules in one process -- reproduced identically on main with these changes stashed, and make test (the project's split test-new/test-legacy runner) passes cleanly; unrelated to this task.\n\nDoD#1 left unchecked for human PR-time verification (behavior-neutral outcome + PR references to decisions/layers.md, decisions/outbound-clients.md, decisions/sdk-typing.md).
<!-- SECTION:NOTES:END -->

## Comments

<!-- COMMENTS:BEGIN -->
created: 2026-07-31 17:06
---
RESEQUENCED per decisions/sdk-typing.md (2026-07-31). The Google directory path is the deepest anti-pattern: execute_google_api_call walks the discovery tree by string and get_google_api_command_parameters SCRAPES method docstrings to validate params. Original plan wired the provider onto google_directory_next (that dispatcher) and deferred rename/contract to TASK-23/25. New plan migrates ONCE, directly onto a factory-built Resource the adapter calls directly, deleting the dispatcher+scraper path for the directory surface and typing at the adapter via domain dataclasses (the discovery Resource is untyped by design - google-api-python-client is maintenance-mode; see sdk-typing.md item 3).

AC CHANGES (human review at task-breakdown checkpoint): AC#1 retargeted from "backed by google_directory_next" to "adapter calls the discovery Resource directly + classify_google_error; no dispatcher/docstring-scraper on the directory path". AC#3 retargeted from "relocate google_directory_next/google_service_next tests" to "classify_google_error coverage; those _next modules left for TASK-23 deletion". AC#2 (health_check parity) unchanged. DoD#1 gains decisions/sdk-typing.md.
---

created: 2026-07-31 17:50
---
RESEQUENCED (2026-07-31, second pass) after decisions/sdk-typing.md's Google-stub revision (TASK-70 now also delivers google-api-python-client-stubs, not just types-boto3). This supersedes this task's "TARGET SHAPE step 1" factory description below: the factory does not just build a RAISING Resource - it also ANNOTATES that Resource's return type using the stub-provided type (e.g. AdminDirectoryResource from googleapiclient._apis.admin_directory_v1, imported under TYPE_CHECKING), giving the adapter's 11 direct Resource calls (service.users().get(...), service.groups().list(...), etc.) real IDE completion and mypy checking - not just the translated dataclass at the far end. This was previously believed impossible (google-api-python-client's discovery Resource is dynamic by design) until the stub package was found during planning; see decisions/sdk-typing.md's "Revised finding" paragraph and item 3.

Practical effect on this task's plan: STEP 1 ("Add/trim the factory + classify_google_error") gains one clause - the factory's get_google_service(...)-equivalent must declare its return type explicitly (`service: AdminDirectoryResource = build("admin", "directory_v1", ...)`), not just call build() untyped. STEP 2's 11 direct Resource calls are typed as a consequence, with zero extra adapter-side work. AC#1 and the task's dependency list were updated accordingly (this task now explicitly depends on TASK-70 in addition to TASK-22.3, since TASK-70 is the task that adds google-api-python-client-stubs as a dev dependency - previously an implicit transitive dependency via 22.3->22.2->70, now made explicit since TASK-70's Google half is a distinct deliverable this task directly consumes).
---

created: 2026-08-07 15:31
---
REASSESSED 2026-08-07 (fresh grounding pass). Corrected a major factual error in the prior plan: GoogleDirectoryProvider does NOT run through the google_directory_next/google_service_next dispatcher today - it runs through infrastructure/clients/google_workspace/{directory.py,executor.py,session_provider.py}, which already calls the discovery Resource directly (no docstring scraping, no string dispatch). The real docstring-scraping anti-pattern (get_google_api_command_parameters) lives in a third, unrelated module (integrations/google_workspace/google_service.py) never touched by this provider. This task's actual job is narrower than previously framed: remove the standing DirectoryClient/GoogleWorkspaceClients facade class per decisions/outbound-clients.md's no-wrapper rule, add google-api-python-client-stubs typing (verified resolvable via a live mypy run), and add a real classify_google_error (today's executor has none - all errors collapse to a generic permanent_error). Also found and preserved a load-bearing per-call OAuth least-privilege scoping behavior (each of the 11 operations requests a different, narrower scope list) that a naive single-Resource-built-once design would have silently broadened; added a new AC requiring this be preserved and tested. health_check gap-check resolved: it already exists today, this task relocates it rather than inventing it. Plan rewritten in full via --plan; SIZE GATE verdict unchanged (fits one PR, and is actually smaller than TASK-22.3's equivalent slice since the call-shape doesn't need reinventing, only the facade tier removal + typing + classification).
---
<!-- COMMENTS:END -->
