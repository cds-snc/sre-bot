{
    "hooks": {
        "PostToolUse": [
            {
                "type": "command",
                "command": "python3 - <<'PY'\nimport json,sys\nobj=json.load(sys.stdin)\nname=(obj.get('tool_name') or '').strip()\nif name:\n    print('[validation] tool:', name)\nprint('[validation] run after meaningful edits: mypy && flake8 && black --check . && pytest')\nraise SystemExit(0)\nPY"
            }
        ]
    }
}